# CryptoNova Testnet App — CLAUDE.md

Read this file at the start of every session before touching any frontend code.

---

## Active state (update after each session)

| Item | Value |
|------|-------|
| **KEEPER RAN OUT OF GAS AND STALLED 3H — RECOVERED SAME DAY (2026-07-30)** | The DO keeper EOA **`0xd419681BA72992636f05e256168681c939826B4b`** (drives `performUpkeep` + `coPayRescue` `0x37fb13fa`; ~77,000 lifetime tx) went SILENT after its last tx at **10:20:34 UTC** — ETH balance drained to **0.0000047 ETH**, so it literally could not send. While down, co-pay rescues and the parked-queue drain stopped, which surfaced as member reports (2× "T4.1 re-entry BLOCKED", the morning's upgrade-too-large batch). **Owner refuelled to ~0.0995 ETH (then topped up more); keeper resumed immediately**, firing every ~2-4s to drain the backlog against T2 MatB `0x16518a1D…7411917`. **The reverted `coPayRescue` calls in that burst are EXPECTED and HARMLESS** — Value 0 ETH, `execution reverted` before any state change, ~0.0000178 ETH gas each: the keeper attempts parked-queue members who are already seated / already rescued / stale-`parkedAt`, and the contract correctly refuses. Rate is amplified during a post-outage drain and self-corrects. **Permanent fix = V8.46 item 6 (clear stale `parkedAt`).** **LESSON: watch keeper ETH balance, not just liveness — a near-zero gas tank IS an outage. Verify keepers via the keeper EOA's latest-tx age + balance on sepolia.basescan.org, since neither the cloud container nor device_bash can reach the RPC.** |
| **ENTERING A TIER WHERE YOU ALREADY HOLD COMMISSION DESTROYS THE BALANCE (2026-07-29 → V8.46 item 8, THE ONLY BUG THAT CAN DELETE MEMBER FUNDS)** | Chased down from "withdrew $1k twice but Total Withdrawn is wrong". Wallet received **$1,970.00 = $2,000 × 0.985**; ledger said **$1,947.50**. Short exactly **$52.50**. Sixteen payouts reconciled (`wallet_inflow.js`), fifteen to the cent; T3.1 MatA paid $51.71 with `totalWithdrawn` **$0.00**. The receipt settled it (`tx_decode.js` on `0xb11eee58`): `withdrawPartial` arg **$52.50**, BOTH fee and `EarningsWithdrawn` emitted, SUCCESS — so :996 DID increment, then something zeroed it. Withdrawal block 44796516 ~21:40 UTC; **`joinedAt` 23:30:02 UTC.** **CAUSE: `_register:313` reads `!hasEverJoined` as "no record exists" and builds a FRESH struct. It actually means "never took a seat here" — `_credit:928` writes `withdrawable`/`totalEarned` for an upline in the matrix where their DOWNLINE entered, and `withdrawCore` gates on `withdrawable > 0` not membership, so such a holder can withdraw too. Entering that tier later overwrites balance, earnings, history and crossing reserve with zero; the USDC stays in the matrix as unattributed surplus.** **THE OWNER ESCAPED FUND LOSS BY ORDERING ALONE — he had already withdrawn, so `withdrawable` was $0 when the reset hit. Enter first and the balance is DELETED.** Exposure = anyone with `hasEverJoined == false && withdrawable > 0`, i.e. every member whose direct went higher than they did. **Fix (field-wise update, never a struct construction) + 5 tests in `V8_46_PLAN.md` item 8; in MatrixLogicLib, which is LINKED so the factory pays nothing. Interim: WITHDRAW BEFORE UPGRADING.** **METHOD NOTE — the win came from changing the question. Scanning `EarningsWithdrawn` across 14 matrices needed ~2,800 `eth_getLogs` at the free endpoint's measured cap (PROBE: 2,000 OK / 9,000 FAIL, "could not coalesce error" = its 429) and returned a confident INCONCLUSIVE. ONE filter on USDC `Transfer(to=member)` answers the same question in 27 chunks, because every withdrawal ends at `withdrawCore:1007`. When a scan is too expensive, look for the single contract every path must touch.** |
| **THE KEEPERS' OWN BLANKET APPROVALS WERE WHAT BROKE MEMBER UPGRADES (2026-07-29, FIXED — biggest operational win of the day)** | Members could not upgrade: estimates came back ~19M against a ~17.8M chain cap. **Cause was ours.** `onCrossToMatB` (TierRouter:1136) auto-upgrades a crossing member whenever **the member's WALLET has a TierRouter allowance** — a path the contract's own comment calls "rare in practice". `pool_primer.js` had been granting standing approvals to every derived wallet, so each one became another link in one transaction's cascade. `cascade_chain.js` walked it: **5 links, every one taking the WALLET path.** `whois_wallet.js` on each: link 1 a real member, links 2–4 **ours**. Revoked **423 wallets / $20,167,625 of allowance** (`revoke_allowances.js`, both HD derivations, `MAX_BAL` guard to skip funded accounts) → chain fell to **2 links**, `gas_probe` **11,778,695**, and the owner walked a wallet **T1 → T10 unaided**. **`pool_primer.js` default is now `TR_MULT=0` and `rr_keeper.js` approves the exact fee JUST IN TIME**, so nothing stands where the cascade can find it. Cron carries `TR_MULT=0` too, belt-and-braces. **DO NOT raise it back: a missing approval costs one keeper attempt, a standing one costs every member their upgrade.** **THE CORRECTION I HAD TO MAKE MID-DIAGNOSIS: I first proposed sizing the approval to the exact next-tier fee. Useless — each link is a DIFFERENT wallet and `onCrossToMatB` only needs `allowance >= fee`. Exact approvals do not shorten the chain; only zero does.** |
| **BASELINE AT THE 2026-07-29 COMMUNITY CALL — keep for comparison after V8.46** | **T10 REACHED ORGANICALLY**, gate 5/5, 18 members through, every tier rotating. Bug list taken from **19 open → 0** in one day; 13 bounties accepted ($13 owed, payment deferred to go-live by owner decision). Eight member-facing frontend fixes shipped to main. Cascade depth 5 → 2. Keeper cron re-cut to a 10-minute cycle, one writer per minute slot, every writer flocked (`manual_rescue` had never had one and shared the keeper wallet with two other jobs). **Constraint that shaped the day: the paid Alchemy endpoint expired, so `sepolia.base.org` carries the load and the testing budget is spent** — watchers were slowed deliberately (`dupe_watch` 7 → 20 min). Owner's plan from the call: **V8.46 deploying in ~48h, full deploy Friday, $30K/day of test funds for two days.** |
| **TOOLS BUILT 2026-07-29 — reach for these before writing a new script** | `member_ledger.js` (per-matrix everything the dashboard sums — FIRST STOP on any wrong-number report) · `ticket_triage.js` (one wallet: seats, duplicates, parked positions with shortfall breakdown, wallet balance, allowance-to-matrix, `reservedFor`, verdict per position) · `cascade_chain.js` (walks a cascade link by link, prints which funding path fires) · `revoke_allowances.js` (bulk zero, both derivations, `MAX_BAL` guard) · `bypass_scan.js` (state test) + `bypass_scan_full.js` (log/tx test, `TARGET=` self-test) · `limbo_check.js` · `gas_probe.js` (estimate + 30M `p.call` to separate "refused" from "too large") · `tier_history.js` · `gate_status.js` · `tester_status.js` · `withdraw_probe.js`. All read-only unless named otherwise; all default to `deployed_addresses_v8_45.json`. |
| **WITHDRAWING REMOVED A MATRIX FROM THE COUNT THAT REPORTS THE WITHDRAWAL (2026-07-29, fixed)** | Owner spotted it mid-call: withdrew ~$1,000 and "Total Withdrawn" barely moved. `totalWithdrawn` and `totalEarned` are **per-matrix** fields, so the headline is a SUM — and the enumeration filter at `index.html:4603` admitted a commission-only matrix on `!hasEverJoined && withdrawable > 0`. **A tier drained to zero then fails BOTH limbs: never joined, no balance. So the act of claiming the money deletes the record of the claim.** `member_ledger.js` on 0xe8Ad7bbA: ten such matrices held **$1,651.00** of history (T1.2 MatA $26 · T3.1 MatB $337.50 · T4.1 A/B $80/$170 · T5.1 A/B $175/$187.50 · T6.1 A/B $225/$150 · T7.1 A/B $250/$50). Dashboard showed **$296.50** against a true **$1,947.50**, and TOTAL EARNED was short by the same amount. Fixed by testing HISTORY not balance: `withdrawable > 0 \|\| totalWithdrawn > 0 \|\| totalEarned > 0`. **TWO SITES — the dashboard loop and the breakdown modal (`:5531`); the comment there already warned they must match, which is the only reason the second one was found.** Same enumeration family as the referral-count bug and the limbo mis-sale: a filter that reads sensibly and silently drops a population. |
| **A RESCUE LOAN CANNOT REPAY ONCE THE MEMBER LEAVES THAT MATRIX — AND THE MONEY IS SITTING NEXT TO IT (2026-07-29 → V8.46 item 7)** | Reported on the call as "Sherwyn's $1.68 loan not repaying despite multiple Tier 1 rotations". **Not a rate problem: `rescueRepayBps` is 10,000 = 100%.** `rescueDebt` is **per-matrix** and clears exactly two ways — a slice of a POOL SHARE (`_settlePool:450`, but `if (share == 0) return;` sits ABOVE the debt block) or from `withdrawable` at cycle-out (`_cycleOutRoot:548`). Both need the member ACTIVE IN THAT MATRIX. A member who moved on earns no share there and will never cycle out of it again, so the debt is frozen — **rotating anywhere else does nothing, because the debt belongs to a matrix they left.** Measured on 0xe8Ad7bbA: T1.1 MatA $2.07 owed / $1.00 there, T2.1 MatA $2.75 owed / **$35.00 withdrawable there**. **`withdrawCore` never reads `rescueDebt`, so they can withdraw the $35 and leave the $2.75 outstanding — the funds to clear it are in the SAME matrix as the debt.** Fix is one deduction in `withdrawCore` after `_settlePool` (patch written in `V8_46_PLAN.md` item 7), which guarantees repayment on the one action a stranded member will still take. Frontend must show the deduction — a withdrawal quietly returning less than quoted is the $10-lock mistake again. |
| **TOOL: `member_ledger.js` — every number the dashboard aggregates, shown per matrix** | Built 2026-07-29 for the two reports above. Per matrix: seated/parked/left/credit, withdrawable, `freeWithdrawable`, crossing reserve, totalEarned, totalWithdrawn, `rescueDebtOf` — then the sums, so a headline can be checked against the parts it claims to add. Also prints WHY each outstanding debt is not repaying. **Reach for this first on any "the number is wrong" report: nearly every one has been a per-matrix value presented as a single figure.** `ADDR=0x… node member_ledger.js` |
| **THREE ACCESS-CONTROL HOLES, AND THE WORST ONE CAN BE USED ON SOMEBODY ELSE (2026-07-29 — V8.46 SCOPE)** | **(1) `MatrixKeeper.performUpkeep(bytes)` :532 is `external` with NO guard.** Anyone can hand-craft a `WorkItem[]` and drive the entire keeper queue. Most work items save themselves: `_doReclaimSlot` needs 7 days idle AND `rotationCount > 0`, `_doForceRotate` needs a genuinely frozen MatB, `_doGhostEntry` is off by default. **(2) `_doEvictParked` :685 is the exception — its ONLY precondition is `parkedAt != 0`. No time gate.** The matrix-level `evictParked` is `onlyKeeper`, but MatrixKeeper IS the keeper, so driving MatrixKeeper satisfies it: `anyone → performUpkeep → _doEvictParkedExternal (onlySelf, satisfied) → matrix.evictParked (onlyKeeper, satisfied)`. **`MatrixLogicLib.evictParked:1273` does not touch funds — it calls `_removeFromParkedQueue`, which zeroes `parkedAt`. Both `selfRescue` (:1185) and `coPayRescue` (:1148) REQUIRE `parkedAt > 0`. So evicting a parked member permanently locks them out of re-entry: the rescue keeper no longer sees them and they cannot rescue themselves. Money is safe and withdrawable; the position is not.** For the price of gas, anyone can do this to every parked member at once (~30 at any moment on testnet). **(3) `register` / `registerWithCoupon` on the matrix — see the bypass row.** **Ranking for V8.46: eviction FIRST — the register hole only lets someone harm their own wallet, this one lets them harm a stranger's.** Fixes: allowlist `performUpkeep` (owner + governance + keeper EOAs + Chainlink forwarder if kept), and give `_doEvictParked` the same idle gate `_doReclaimSlot` already has. |
| **⛔ CORRECTED 2026-08-22 — `0xdb9B1e94…` IS METAMASK'S DelegationManager, NOT CHAINLINK. THE OWNER WAS RIGHT.** | **This row previously read "CHAINLINK AUTOMATION IS STILL LIVE AND SPENDING LINK" and said the chain disagreed with the owner. It did not.** `0xdb9B1e94B5b69Df7e401DDbedE43491141047dB3` is **MetaMask's `DelegationManager`** — selector `0xcef6d209` = `redeemDelegations(bytes[],bytes32[],bytes[])` and its one event = `RedeemedDelegation(address,address,(address,address,bytes32,(address,bytes,bytes)[],uint256,bytes))`, **both keccak hashes recomputed and matched exactly.** Confirmed independently: `typeAndVersion()` REVERTS (every Chainlink contract implements it), **0 Chainlink events across 123 logs** in a sampled tx (a registry performing an upkeep always emits `UpkeepPerformed`), and the owner holds **no funded LINK account**, without which no upkeep can execute at all. So the "63 seatings" were **MEMBER REGISTRATIONS through MetaMask smart accounts** — delegated execution, which the project has used all along (V8_50_HANDOFF 29.7 records the deployer as formerly EIP-7702 delegated to "MetaMask's stateless delegator"). ⚠ **CONSEQUENCE: a registration's `tx.from` is a MetaMask redeemer, NOT the member.** Any census, fraud check or leaderboard attributing by `tx.from` will mis-credit these. **⛔ THE ORIGINAL LABEL WAS A GUESS THAT WAS NEVER VERIFIED, AND BECAUSE IT SAT IN THIS FILE IT WAS QUOTED AS FACT BY THREE LATER SESSIONS** — one of which built a capacity-cliff argument and a hold on the V8.50 community deploy on top of it, and argued against the owner's correct recollection three times. **A LABEL IN CLAUDE.md IS A CLAIM, NOT A MEASUREMENT.** Full account: V8_50_HANDOFF 30.5d / 30.5e / 30.5f. |
| **THE DASHBOARD INVOICED UPLINES FOR TIERS THEY NEVER JOINED — AND THE BUTTON WORKED (2026-07-29, frontend FIXED, contract fix pending)** | **`FigureEightMatrixV8.register(address)` is `external` with NO access control** (:396), as is `registerWithCoupon` (:409). Anyone holding the fee can seat themselves in ANY tier's matrix, skipping TierRouter entirely: `memberHighestTier` never advances, `_checkTierFirstEntry` never runs, `_upgradeEligible` is never consulted, tier progression is not required. **Our own UI walked members through that door.** `index.html:4684` decided "reclaimed-limbo member" from `not seated && parkedAt == 0 && isMatA` with **no history test**, and the enclosing guard at :4581 admits a matrix on `hasEverJoined` **OR `_hasCredit`** — and referral commission is credited into the matrix where YOUR DIRECT entered (`MatrixLogicLib._credit:928). So an upline accrues balances in tiers they have never occupied, and every one looked exactly like a cleared slot. `limbo_check.js` on **0x7CAFC198: TEN false offers, T1.2→T10.1, totalling $19,435 — precisely a full T1→T10 climb.** Panel read *"Your T10 Matrix A slot was cleared… Re-Entry Fee $10,000"* and the button called `register()` straight on the matrix. **MEASURED BLAST RADIUS (`bypass_scan_full.js`, full 11-day history, 25,306 seatings): 6 transactions, 8 seats, ~$20,025, blocks 44752308–44781263.** Members: 0x1e8e2dcf (Sherwyn), 0x0f509981 (Kira), 0x536685f0, 0xd6fbdf7a, 0x0af85760, 0x1acc0225, 0x5816e46a, 0x84a4d33a. **No funds lost, no positions corrupted, and the whale gates are SAFE — uncounted entries make a gate open LATE, never early.** Frontend fixed (commit 77c26ed: `&& member.hasEverJoined`). **V8.46 MUST add `onlyTierRouter` to both `register` and `registerWithCoupon`** — `_onlyTierRouter()` already exists at :195 so it costs no new revert string, but the bodies assume `msg.sender` IS the member, so the honest fix is to delete them and route through the router's existing equivalents (`routerCouponEntry` :419 is the model). **`doLimboReEntry` in the frontend calls `matA.register()` and will need a router-side replacement.** |
| **0x84A4D33A: SEATED IN T10, RECORDED AS T1 — the anomaly that exposed all of it** | Dashboard showed T10 Matrix A #103, $5,000 crossing reserve, T1–T9 all "Graduated", "Auto re-entry: $$10,000" beside "Total reserved: $35.00". Every number was correct and they described **different tiers**: the items are computed locally from `highestTier`, the total is overwritten by on-chain `reservedFor` (:5215), and `reservedFor` derives from `memberHighestTier` — which was **1**. `tier_history.js` proved it: 35 router events, all tier 1 (registered, 17 crossings, 14 re-entries), **zero upgrade events of any kind**, and the seating tx `0xde428a63` went `from` the member `to` the T10 matrix with selector `0x4420e486 = register(address)`. **Lesson: when a screen shows something physically impossible, stop reasoning and fetch the transaction.** Two related frontend bugs found alongside: `$${fmt6(x)}` prints `$$10,000` (fmt6 already prepends `$`, three sites at :5200/:5204/:5208), and `nextFeeRaw = window._dashUpgradeFee \|\| 25_000_000n` falls back to **T2's fee for a T10 member** because the upgrade panel that sets that global never runs at the top tier. |
| **A DETECTOR THAT REPORTS ZERO MUST FIRST FIND A PLANTED POSITIVE (2026-07-29)** | `bypass_scan_full.js` confidently printed **"0 direct-entry seats"** twice while a bypass we had ALREADY PROVEN BY HAND sat inside the scanned range. **v1:** filtered on "does this tx contain ANY TierRouter log" — but a direct `register()` into a full matrix triggers a rotation whose cascade emits router events about **other** members, so the bypass was explained away by somebody else's event. Fixed by matching **per member** (topic1). **v2:** candidates are iterated in block order and `MAXTX` capped the fetch at 400 of 5,562 — the known case sat at **position 5,561**. Both failures produced a clean, plausible, wrong answer. The script now takes `TARGET=0x…` and self-tests against a known positive before its result is worth anything. **Companion lesson from the same hour: the FIRST scan (`bypass_scan.js`, a state test comparing `memberHighestTier` to the seated tier) found 1 of 726 and was not wrong — it measured the currently-seated population, and 5 of the 8 had since climbed past their bypassed tier legitimately, which repairs `memberHighestTier` and erases the evidence. Two scans, two methods, agreeing only once you know what each can and cannot see.** |
| **EVERY SEATING SELECTOR, ACCOUNTED FOR (2026-07-29 — keep this table, it is the baseline for the next audit)** | 25,306 seatings into T2–T10 over the full V8.45 history. `0x37fb13fa` **coPayRescue** 4,242 · `0xb62847e9` **selfRescue** 1,068 · `0xfed7e53b` **manualUpgrade** 119 · `0xcef6d209` **`redeemDelegations` on MetaMask's DelegationManager `0xdb9B1e94…` — member registrations via delegated execution, NOT automation** 63 ⛔ *(this entry read "Chainlink Automation registry" until 2026-08-22; the label was never verified and was wrong — see the corrected row above)* · `0xcb1de45b` **bulkUpgrade** 27 · `0x4585e33b` **performUpkeep** 18 · `0x240d6f87` **adminForceRotateRoot** 11 · `0x4420e486` **register() to a NON-matrix** (T1 entry cascading upward) 8 · `0x4420e486` **register() TO A MATRIX = THE BYPASS** 6 txs / 8 seats. The router/bulk/manual entries appear in the "unexplained" bucket legitimately: member A's upgrade fills a matrix, the rotation re-seats member B, and B's seat has no router event naming **B**. |
| **$10 WAS HARDCODED AS "THE ENTRY FEE" IN THREE PLACES (2026-07-29, fixed)** | $10 is **T1's** fee and nothing else, but the withdrawal loop (`doWithdrawPartial`), the headline "Available USDC" (`realW`) and the per-tier rows all did `withdrawable − 10_000_000n` for a seated member. The real lock is `ENTRY_FEE − crossingReserve` (`withdrawCore`:976) **plus the whole automation reserve** (:986) — up to **$2,500 at T9**. So the site quoted money the contract would never release and `withdrawPartial` reverted into "Transaction failed on-chain". **That is Sherwyn's "$500 from $514 available".** Measured on a live wallet: headline **$42,916** against **$5,260** genuinely locked. All three now call **`freeWithdrawable()`** (V8.44 G3), which is the contract's own answer, accrual included. **THE REASON IT SURVIVED: the per-tier rows deliberately mirrored the headline's wrong arithmetic "so rows sum to the headline". Two wrong numbers agreeing looks exactly like two right ones.** |
| **A WALLET RE-ESTIMATES AND IGNORES YOUR gasLimit — SO NO FRONTEND GUARD CAN STOP A DOOMED TX (2026-07-29, MEASURED)** | `gas_probe` on `0xdE5fe7cB` T2→T3: our estimate **14,903,722** — *under* the 15M refusal line, so the frontend correctly let it through and set 15.6M. The transaction the wallet actually built carried **20,067,248 = 19,111,664 × 1.05**: the wallet's OWN estimate, taken a moment later, in the expensive mode. **Both numbers are real — the cost is bimodal and the wallet samples it at signing time, after we hand off.** Consequence: the ceiling checks stop *us* sending doomed transactions but cannot stop the wallet, so `friendlyError` now decodes "gas limit too high"/-32003 into a plain explanation that **retrying works** (it does — the next attempt often lands in the quiet mode). **THE REAL HEADLINE: T2→T3 costs 14.9M in the QUIET case, which after Rabby's ×1.15 is 17.14M against a ~17.8M cap — 660k of headroom on a $50 upgrade.** Keeper wallets do the identical move for 1.9M because they are fresh. **V8.46-B is no longer optional.** |
| **FRONTEND ABI SAID `referrer` WAS UN-INDEXED; THE CONTRACT INDEXES IT (2026-07-29, fixed)** | `event MemberRegistered(address indexed member, uint8 tier, address indexed referrer)` — V8.34 indexed `referrer` *specifically* so uplines could be queried by topic. `index.html` still declared it un-indexed, so ethers looked for it in the log DATA where only `tier` lives, decoding failed, `e.args.referrer` came back undefined, and **every** direct-referral lookup fell through to the scan fallback. That fallback counts members **currently seated** in T1's pairs + T2/T3 pair 1 — so a direct who upgraded past T3, cycled out, parked, or landed in pair 2 is invisible. **@Lavern_Gay's four directs displayed as one.** Fixed: filter server-side with `filters.MemberRegistered(null, addr)`, chunk the range if the provider caps `eth_getLogs`, and the positional scan now **always** reports `incomplete: true` because it is a floor, never a total. **Same source-vs-chain drift as the 375/400 row, in the opposite direction: the contract changed and the frontend never picked it up.** |
| **SELF-RESCUE'S SHORTFALL IS PAID FROM THE WALLET, NOT FROM EARNINGS (2026-07-29)** | `MatrixLogicLib.selfRescue:1207` does `cfg.usdc.safeTransferFrom(member, address(this), shortfall)` — a low in-matrix balance does **not** block a rescue, it just means the difference comes from the member's own USDC, approved **to the MATRIX, not to TierRouter**. My first draft of the shortfall message told the member to wait for earnings; it would have sent someone away when one approval finished it. Also: `const SELF_RESCUE_ABI = ['function selfRescue() external']` could not decode the OZ ERC20 custom errors, so an allowance failure surfaced as "Transaction failed on-chain — hard-refresh". **Sherwyn reported that twice and concluded it was "a testnet thing".** Both fixed. **`ENTRY_FEE`, NOT `entryFee`** — it is `uint256 public immutable ENTRY_FEE` (FigureEightMatrixV8:44); the first version of the check called a getter that does not exist, reverted, and was swallowed by its own fall-through, so the fix shipped inert for twenty minutes. |
| **FIVE BUGS TONIGHT, ONE SHAPE: A VALUE ASSUMED INSTEAD OF ASKED FOR** | `$10` for the lock · `entryFee` for `ENTRY_FEE` · `hasEverJoined` for a getter that is really `isParked` · a gas estimate clamped with `Math.min` · an event arg declared un-indexed. **In every single case the contract had the exact answer available in one call.** Related: `withdraw_probe.js` v1 batched five reads in one `Promise.all` under `catch { continue; }`, two of the five did not exist, and it printed "$0.00 everywhere" for a member asking about $514 — **a swallowed read impersonating a finding, in the script written to diagnose exactly that.** RULE: before using a getter, grep the contract for it. Never batch unknown getters. A failed read prints "?" and its reason, never a number. |
| **THREE KEY BASES IN TierRouter — AND THE VELOCITY GATE IS THE ODD ONE (2026-07-28 night)** | `tierPairManagers`/`tierEntryFees` are `[10]` arrays, **INDEX 0..9**. `tierFirstEntries`/`tierGateThreshold`/`tierWhaleGateActive` are mappings keyed by **TIER NUMBER 1..10** (`_checkTierFirstEntry(member, tierNum)`). **`tierVelocityGreen` is a mapping keyed by INDEX 0..9** — the constructor fills it `for (uint8 i = 0; i < MAX_TIERS; i++)` (TierRouter:391) and the setter names its parameter `tierIndex` (:546). `gate_status.js` v1 read all four mappings with the 1..10 key, so **every velocity value was reported one tier low and T10's row read a slot that has never been written** — printing a confident "PAUSED" for a key that does not exist. **THE TELL, ignored at first: T10 showed velocity PAUSED beside a coherent whale gate of 0/5. One row cannot be half-initialised.** Fixed; the header of that script now carries the three-base table. |
| **VELOCITY GATE PAUSES THE NEWEST TIER — BY DESIGN, AND IT FIGHTS `onCrossToMatB`** | `MatrixKeeper._doVelocityCheck` (:711) sets each tier green/red on **recent entry count vs `velocityThreshold`** in a rolling window. A brand-new tier is slow, so it gets paused, which blocks auto-upgrades INTO it (`_executeAdditive`, TierRouter:1291), which keeps it slow. Meanwhile `onCrossToMatB` (:1111) flips the SAME flag green on every MatB entry. **Two mechanisms write one flag on different schedules and the periodic check wins.** Measured 2026-07-28 23:xx: T9 and T10 both PAUSED while T1–T8 were green. **NOT a blocker for the T10 push — velocity gates only the AUTOMATIC upgrade; manual/keeper upgrades and the rule-(c) MatB-seat route ignore it entirely**, which is why T9 kept filling at ~25 entries/hr while paused. Do not "fix" this by forcing the flag; it will be overwritten on the next check. |
| **ROUTE TO T10 — ALL THREE ELIGIBILITY ROUTES ARE THE SAME ROAD (2026-07-28)** | Owner asked to let T10's gate open organically like T6–T9 did. Correct call, and forcing it would have been worse than it looks: `_checkTierFirstEntry` returns early when the flag is already set (:1488), so `setTierWhaleGateActive(10,true)` **freezes `tierFirstEntries[10]` at 0 permanently** — members entering T10 while forced open are never counted and flipping it back does not recover them. **The real sequence:** T9.1 MatA fills to 127 → one more entry rotates it → root cycles out of MatA and crosses into T9 MatB → `onCrossToMatB` flips T10's velocity green **by itself** → that member now satisfies `_upgradeEligible` rule (c) and can manualUpgrade into T10 for $10,000 with no gate → **5 such entries open the gate organically**. **STEP 4 IS NOT AUTOMATIC: a MatA cycle-out does NOT run the additive engine** (`_cycleOutRoot` sends MatA roots to `_crossToPartner` only), so somebody has to press the button. Tool: `gate_status.js` prints this sequence with live numbers. |
| **DASHBOARD IMPLEMENTED 2 OF THE CONTRACT'S 3 UPGRADE RULES (2026-07-28, fixed + shipped all 3 branches)** | `_upgradeEligible` (TierRouter:862) accepts **(a)** a completed cycle in the previous tier, **(b)** the target's whale gate being open, or **(c)** a seat in ANY of the previous tier's MatBs. `index.html:4875` checked (a) and (c) only. **A member seated in a FULL MatA with no cycle yet — which is all 127 of T8.1's MatA — was eligible via (b), the contract would have accepted `manualUpgrade`, and the panel hid the button.** The only control left was the bulk card, and `showBulkHigh` targets `highestTier + 1`, so for every T5–T9 member "bulk" spanned exactly ONE tier: the same call, and the one that was failing. `tester_status.js` confirmed the blast radius — **7 of 10 funded T8 wallets had `gate` as their ONLY eligibility route.** Bulk now shows only when it spans ≥2 tiers. **Same shape as the V8.43 contract bug at TierRouter:851, mirrored into the frontend: two buttons, two different eligibility rules, one wrong.** |
| **NEVER CLAMP A GAS ESTIMATE DOWNWARD (2026-07-28)** | Three sites did `Math.min(est * 1.05, 16_500_000)`. On a genuine 17.7M estimate that sends 16.5M — which does not make the tx cheaper. The wallet then pads ITS own estimate and the RPC refuses with **"[From sepolia.base.org] gas limit too high"**, which members read as the site being broken; had it been accepted, the clamp would have bought an out-of-gas revert instead. **A clamp converts "too expensive to send" into a failure with no explanation.** All three now refuse above **15M** (chain cap ~17.8M ÷ wallet padding up to 1.15) and print the actual figure. Also removed the empty catch that fell back to a static 16M — **a reverting estimate is the contract saying the call cannot succeed; sending anyway costs the member gas and discards the reason.** |
| **UPGRADE GAS IS BIMODAL — AND `bulkUpgrade` IS NOT THE EXPENSIVE ONE (2026-07-29, MEASURED)** | `gas_probe.js` on a live T1 wallet: `manualUpgrade` **12,949,191** · `bulkUpgrade` **12,949,689** — **498 gas apart.** The "bulk is a heavier function" theory (mine, stated twice) is **WRONG**. But the same wallet's own tx minutes earlier carried gas `0x1285473` = **19,420,275 = 18,495,500 × 1.05**, i.e. the wallet estimated ~18.5M. **So the cost is BIMODAL: ~12.9M when the entry just takes a seat, ~18.5M when the entry is the one that fills MatA and triggers the rotation cascade.** T2.1 turns over constantly (13,359 MatB rotations), so which one a member gets depends on the second they press the button. **This is the honest explanation for "upgrades fail sometimes on the same wallet" — not flaky RPC, but whether you were entrant #128.** Retrying genuinely works. **A $25 upgrade already consumes 73% of the chain's capacity in the QUIET case** — revisit the 15M refusal threshold only after V8.46-B's depth cap ships, since raising it now just re-enables doomed sends. |
| **rr_keeper JOB D FUNDED $450,000 AND UPGRADED ZERO (2026-07-28, fixed)** | Run #252: **17 wallets topped up at ~$27,500 each, "38 candidates checked, 0 upgraded"**, whole 170s slot wasted. Cause: `target = Math.min(ceiling, highest + MAX_SPAN - 1)` aimed every T8 wallet at **T10**, and `bulkUpgrade` requires the gate for **EVERY tier in the span** (TierRouter:1044), not just the first — so a shut top tier poisons the whole call, and the funding step sits ABOVE the attempt. **Same shape as the allowance bug fixed that morning: money spent on a candidate that could never use it.** Fix: read the gates once per tick, walk the target DOWN to the highest open tier, and skip **before funding** when nothing is reachable. T8 now aims at T9 — the exact entry T9.1 MatA needs. **Job C is deliberately NOT gate-checked: it enters one tier, where the contract's rule is three-way and a cycle or MatB seat also qualifies.** |
| **PAIR NUMBERING** | **The dashboard numbers pairs from 1 (T1.1/T1.2/T1.3); the contracts index them from 0.** So dashboard T1.3 == `pairs[2]`. All keeper/diagnostic scripts were switched to 1-BASED display on 2026-07-27 to match what you and the members see — the index passed to contract calls is still 0-based. Anything written before that date in this file or in logs may be off by one. |
| **TIMEZONE** | **Owner is EDT (UTC-4). Work in EDT, not UTC** — UTC makes evening look like the middle of the night. VPS commands: prefix `TZ=America/New_York date`. |
| **LIVE CONFIG CHANGES 2026-07-26 PM EDT** | **MatA rotation had STOPPED on every saturated pair in all 10 tiers** — members frozen in seats 2..127 permanently (Sherwyn reported it across 3 versions; Kira, CryptoJan22, Lavern corroborated). Fixed with TWO reversible owner calls, **no redeploy, no reset, nobody lost a position**: (1) **8:25 PM** `TierRouter.setPairExpansionThreshold` 381 → **1000000** (block 44671830) — **PERMANENT, do not revert**; revived T2.1/T3.1/T5.1 MatA. (2) **8:50 PM** T1 `PairManager.setEntryThresholds` route 381 → 3000 (block 44672565) — **now managed automatically by the round-robin keeper**. T1.1 MatA verified 254 → 263 at ~26 rotations/hr. Audit trail: `/root/keeper/threshold_changes.log`. Revert commands are logged with every change. |
| **Round-robin keeper (NEW)** | `/root/keeper/route_rr.js`, cron `*/5` + flock. Walks the new-member entry stream around T1's pairs so no MatA starves. **Entry-driven, not time-driven** (QUOTA=8 entries, MAX_MIN=30 fallback) because the stress keeper delivers bursts every 30 min. Kill switch: `touch /root/keeper/route_rr.OFF`. Master copy: CryptoNova-Keepers/route_rr.js. Pairs with internal churn hand off fast; starved pairs hold — that allocation is intentional, don't "fix" it to strict fairness. |
| **V8.46 BUILT AND GREEN (2026-07-28) — 410 passing, 0 failing** | Three parts, all from measured evidence rather than inference. **(1) PREVENTION — `MatrixLogicLib:255`:** a seat is refused when the member holds the PARTNER. Placed at the one point a seat is actually taken, so rescue, upgrade, crossing and registration are covered by ONE guard instead of six patched call sites — today alone turned up four one-sided guards nobody knew existed. Reuses the existing `"F8V8: already in matrix"` string deliberately: `MatrixKeeper:558` already treats it as expected-and-swallowable on the parked-rescue path, so a co-pay that would duplicate is skipped cleanly. **(2) CONTAINMENT — `_cycleOutRoot`:** checks the destination before `_crossToPartner` and PARKS instead of letting the revert escape and kill a stranger's transaction. Prevention stops NEW duplicates; this is what protects the pair from the ~20 already live and from any cause still unknown. A pre-check, not a try/catch — `_crossToPartner` is an internal library call and Solidity cannot catch those. **(3) DOUBLE ROUTING — `PairManagerV8.freePairFor`:** the second seat goes to a DIFFERENT pair (owner's call). V8.45 sent it to the other half of the member's own pair, so the feature was manufacturing the bug. SKIPS rather than reverts when no pair is free — the double runs inside the cycle-out, so a revert would roll back the re-entry and upgrade that already succeeded in the same call. **SIZES: TierRouter 24,541 (35 spare) · MatrixPairFactory 24,464 (112) — `scripts/sizes.js` prints them; run it after EVERY edit.** The factory embeds FigureEightMatrixV8's CREATION code, so a change to the matrix silently inflates the factory: one extra revert string put it 75 bytes over. **Libraries are LINKED, not embedded — that is why the guard lives in MatrixLogicLib and costs the factory nothing.** Tests: `V8_46_PairGuard.test.js` G1/G1b/G2/G3. G2 builds a PRE-FIX duplicate via a decoy partner (no supported path can create one now) and asserts a stranger's `register()` still succeeds. **Two suite failures were RPC contention, not code — both passed in isolation (FigureEight MSIZE=127 in 2,993ms against a 40s timeout).** |
| **THREE GETTERS THAT OVER-PROMISE — AND STALE `parkedAt` (2026-07-28)** | Every wrong answer today came from a getter whose name meant more than it checked. (1) **`tierPairManagers(uint8)`** — the getter is `uint256`; the uint8 ABI is a different selector for a function that does not exist, and an empty catch turned the revert into "T2–T10 issued no loans". (2) **`isInMatrix`** — per-MATRIX, not per-PAIR; six guards tested one half and 67 duplicates formed. (3) **`isParked()` = `hasEverJoined && !isInMatrix`** — true of ANYONE who ever joined and moved on, so seat_audit reported a member as parked in BOTH halves of two pairs at once. (4) **`parkedAt` is a leftover timestamp, not a live flag**: `_removeFromParkedQueue` (MatrixLogicLib:1198) clears it only when the member is FOUND in the `parkedMembers` array, so a re-seating path that misses that leaves it set — Kira reads as seated in T3.1 MatA *and* parked there. **The reliable test is `parkedAt > 0 && !isActiveInMatrix`.** **Members are unharmed** (`selfRescue` checks `!isInMatrix` too) **but the parked QUEUE still holds them, so the copay keeper spends attempts rescuing seated members and fails with "still in matrix" — swallowed at MatrixKeeper:558. Cheap V8.46 fix: clear `parkedAt` unconditionally on any successful seating.** **THE TELL, every time, was a result that made no physical sense** — 7,687 people in 254 seats, parked in both halves at once, seated and parked in the same matrix. Believe the impossibility before the getter. |
| **V8.46-B: CASCADE DEPTH IS GATED BY MEMBER WEALTH — WHY NO FIXTURE REPRODUCES IT (2026-07-28)** | Three fixtures failed to reproduce the live 17.76M, and each failure taught something. **(1)** First measured the worst gas across ordinary registrations — but a registration's cascade depends on whoever is at position 1, not on the victim, so shallow and deep runs returned BYTE-IDENTICAL gas. **Two configurations producing identical gas is not a measurement, it is a constant, and a constant means the lever is not connected to what you are measuring.** **(2)** Second seated the victim at the root — still identical, because **a MatA cycle-out does NOT run the additive engine**: `_cycleOutRoot` sends MatB roots to `handleCycleOut` (re-entry → upgrade → double) and MatA roots merely to `_crossToPartner`. Depth is not one member's tier span; it is a CHAIN THROUGH DIFFERENT MEMBERS — an entry rotates a MatB root, whose cycle-out upgrades into the next tier's full MatA, which rotates ITS root, and onward. **(3)** Third varied the number of full tiers and finally moved: 1 tier 1,939,778 → 2 tiers 1,968,577 (**+28,799**) → 3 and 4 tiers **+0**. **THE CHAIN STOPS AT TIER 2, AND FLAT FEES DO NOT HELP** (identical numbers with `FLAT=1`). **Cause: `_executeAdditive` upgrades only when `escrow + withdrawable >= nextFee`, and the crossing reserve is exactly 50% OF THE FEE — so a freshly seated filler is always ~half short no matter how the fees are scaled.** **CONCLUSION: every link in a deep cascade requires that root to have EARNED the other 50%. Fresh fixtures can never produce it; production produces it after days of accrual — which is exactly the profile of the two members who hit it (Lavern and the owner, six tiers each), and exactly what the stress keeper manufactures by funding wallets up the ladder.** **THE FIX FOLLOWS FROM THIS: if depth is bounded by wealth it can equally be bounded by a COUNTER — thread a depth argument through `handleCycleOut` and stop the additive engine after N links. That caps gas regardless of how rich the chain gets and is testable directly (assert the counter stops at N) with no wealth fixture at all.** Fixture: `V8_46_LadderGas.test.js` (`TIERS`/`SIZE`/`FLAT` env). It does NOT reach the production number and says so — kept as the record of what the mechanism is not. |
| **V8.46-B IS NOT DEAD — A LIVE SELF-RESCUE ESTIMATED 17,762,199 GAS (2026-07-28)** | Owner's own parked wallet: `selfRescue.estimateGas` returned **17,762,199**, against a Base Sepolia per-tx ceiling of **~17.8M**. Rabby's re-estimate (×1.15 = 20,426,529) was refused outright — `"[From https://sepolia.base.org] gas limit too high"`. **The rescue cannot be sent at all, and clamping the limit to fit would only trade a refusal for an out-of-gas revert that costs the member their fee.** This is the measurement CLAUDE.md recorded as MISSING: `V8_46_CascadeGas.test.js` measured **4,651,492** for a single-pair cascade and B was declared dead on that 3.8x headroom — but that harness never drove the **multi-tier ladder** (re-entry → upgrade into the next tier → double seat, each with its own rotation), which is what the old "~15.5M full-matrix cascade" note meant. 17.76M is that path, measured on production. **REOPEN V8.46-B.** Frontend now refuses above 15M with a plain explanation instead of sending a doomed tx, and the outer catch no longer prints "Rescue failed: rejected" (that string is the WALLET reporting an un-creatable tx, not the contract refusing the member). **Still to build: a multi-tier fixture that reproduces 17.76M in the harness.** |
| **A LIVE SETTER CALL IS NOT A CODE CHANGE — 375/400 EXISTED ONLY ON-CHAIN (2026-07-28, owner caught it)** | After setting 375/400 live on nine tiers, `PairManagerV8.sol:138` still read `routeEntryThreshold = 400`… no — it still read **381**, and grep found **no `400` anywhere in the contract**. Every one of those nine owner calls would have been silently reverted by the next redeploy, and the frontend copy describing 375/400 would have become a lie. Owner asked "I do not believe that is written into code yet — please verify". It was not. **Source default now 400, with the reason and the on-chain history in-line; stale `127×3` comments at :359 and :516 corrected.** **RULE: when a live setter changes system behaviour, change the SOURCE DEFAULT in the same session, or write down explicitly that you did not. On-chain state and source drift silently and only a redeploy reveals it — by which point the behaviour has already changed under you.** Same family as the stale `ADDRESSES_FILE` defaults and the VPS copy of `topup_sf.js` that was 12 versions behind: two copies of one fact, no mechanism to keep them equal. |
| **ENTRY THRESHOLDS NOW 375/400 ON T2–T10 (2026-07-28, owner call)** | `PairManagerV8.deployEntryThreshold` stays **375**; `routeEntryThreshold` **381 → 400** on every tier T2–T10 (block 44738582 onward, gas 33,346 each). **Source default updated to 400 the same day — see the row above.** **Why:** the gap between the two is the window the factory has to deploy the next pair, wire its partner and have it ready to receive. At 375/381 that window was **SIX ENTRIES** — seconds under the stress keeper — and pair expansion has already caused two incidents (the T1.2-at-254 freeze needing `adminForceRotateRoot`, and the 2026-07-28 wedge where the upgrade guards went blind the moment a second pair existed). 375/400 gives **25 entries** of headroom. Nothing in the contract depends on 381 being 127×3 — it is documentation (comments at PairManagerV8:130-137 and :496), not logic. **Forward-looking only:** T2.1–T5.1 are already at thousands of entries, far past both values, so nothing was un-saturated. **T1 DELIBERATELY EXCLUDED — its `routeEntryThreshold` is driven by the round-robin keeper (~3000 and moving); setting it by hand fights the keeper and the keeper wins on its next tick.** Tool: `set_entry_thresholds.js` (read-only default · `DRY_RUN=1` · `TIERS=T8` for one · refuses unless BOTH values passed, since the setter takes them together and passing one silently resets the other · warns when a pair is already at/over the new route value, because LOWERING a route threshold instantly saturates a pair — the 2026-07-26 freeze in reverse). Revert commands per tier in `/root/keeper/threshold_changes.log`. |
| **PAIR WEDGE: A ROOT HOLDING BOTH HALVES STOPS THE WHOLE PAIR (2026-07-28) — DETECTED, REPAIRED LIVE, WATCHDOGGED** | Members reported `T3 upgrade would fail: F8V8: already in matrix` and a "host" of unrelated-looking failures. **Cause: a member seated in BOTH halves of one pair.** Harmless until they reach position 1 — then the rotation cycles them out of MatA, `_crossToPartner` tries to seat them in the MatB they already occupy, `require(!isInMatrix)` (MatrixLogicLib:255) reverts, and **that revert is NOT inside a try/catch**. It propagates out and kills whatever transaction triggered the rotation — a stranger's `register()` or upgrade. **Exactly the DoS `V8_46_SeatCollision.test.js` predicted on 2026-07-27 and I recorded as hypothetical.** Measured: **25 live duplicates** (T2.1×12, T3.1×5, T4.1×7, T6.1×1; ZERO in all four T1 pairs), of which **2 were at root and hard-stopping T3.1 and T4.1**. **NO FUNDS MOVED — these revert before any state change.** **REPAIRED LIVE, no redeploy:** `unwedge.js` borrows the keeper slot on ONE MatB (`setMatrixKeeper(us)` → `softParkIdle(member)` → restore in a `finally`), freeing the duplicate MatB seat. Free **MatB, never MatA** — the member keeps their MatA root position, cycles out normally and the crossing re-seats them; freeing MatA would cost them the root seat they were about to be paid from. `softParkIdle` on the MATRIX has **no idle gate** (the 7-day `extendedIdleTimeout` lives in `MatrixKeeper._doReclaimSlot`, not there), so no global parameter is touched. Verified: reserve moved into withdrawable ($346.51+$25.00 → $371.51), keeper restored, root unchanged, 48 previously-reverting upgrades succeeded immediately after. **Clear ONLY the root duplicate, reactively** — freeing a non-root duplicate just parks them and the rescue keeper re-seats them into MatB while they still hold MatA, recreating it. Watchdog cron `4-59/10` runs `LIVE=1 unwedge.js` (no-op when clean). Tools: `seat_audit.js` (`ROOTS=1` wedge check · `DUPES=1` population · `SCAN=1` blast radius · `ADDR=` one member), `whois_wallet.js`, `unwedge.js`. |
| **THE ONE-SIDED SEAT GUARD — FOUR SITES, ALL CHECK ONE MATRIX OUT OF A WHOLE TIER (2026-07-28)** | `_manualUpgrade:825`, `hybridUpgrade:854`, `bulkUpgrade:986` and `_executeAdditive:1185` all test `tierMatrixAAddr[i]` (+`tierMatrixBAddr` in the last). **`setTierMatrices` is `onlyOwner` and was set once at deploy, so those addresses are PAIR 1 FOREVER while `pairCount()` grows.** T1 now has 4 pairs and T2–T6 have 2 each, so a member seated in pair 2+ is invisible to every one of those guards. **Currently harmless only because T2.2–T6.2 are deployed but EMPTY** — a landmine, not yet a victim. Fix must iterate `pairCount()`/`getPairAt()`, or compare against the PairManager, never a single stored address. **ALSO in `bulkUpgrade`: the fee loop (:976-980) sums EVERY tier from `startIdx` to target, but the seating loop (:984-989) `continue`s past tiers the member already holds — so the member is charged for a tier they are not seated in.** Latent today because the skip never fires; **becomes a live overcharge the moment the skip is fixed. Both must ship together.** |
| **SOLVED 2026-07-28: RESCUE CREATES THE DUPLICATES — 88% OF THEM** | `dupe_watch.js` caught formations AS THEY HAPPENED and recorded the entry point of each. **67 formations, 17 members, blocks 44723866–44742566:** `coPayRescue` **52** · `selfRescue` **7** · `manualUpgrade` **6** · `performUpkeep` **2**. **THE UPGRADE PATH IS EXONERATED: 57 of 67 formed in a tier BELOW the member's highest and ZERO above** — an upgrade always seats you ABOVE your highest, so it cannot be the main source. **MECHANISM:** `MatrixLogicLib.selfRescue:1108` and `coPayRescue:1071` both `require(!self.members[member].isInMatrix)` — that is **THIS** matrix's state. The rescue then seats them through `_finalizeCrossing:636` into the **PARTNER** (MatA→own MatB; MatB→own pair via `rescueReentry`). **The destination is never checked.** A member parked in one half while still seated in the other is rescued straight into a duplicate. Same one-sided guard as the four upgrade sites — in the path that does 88% of the damage. **V8.46 SCOPE CHANGED: the guard belongs on the RESCUE re-entry path, not (only) on `_executeAdditive`'s upgrade branch — the fix as written on 2026-07-27 would not have prevented a single one of these 59.** Note the fork replay found **no revert and no failed frame** — correct, because nothing fails: the duplicate forms cleanly and silently, and only bites later when the holder reaches a root. Detector: `dupe_watch.js` (incremental, records tx + selector; `BACKFILL=1` classifies old records). |
| **PUBLIC ARRAY GETTERS TAKE uint256 — AND A SILENT CATCH TURNS THAT INTO A LIE (2026-07-27)** | `tierPairManagers` is `address[MAX_TIERS] public` (TierRouter:149), so Solidity's generated getter is **`tierPairManagers(uint256)`**. Declaring `tierPairManagers(uint8)` in an ABI is a **different selector for a function that does not exist** — the call reverts. In `status.html` that revert sat inside `catch(_){}`, so the pair-manager list silently stayed `[T1pm]` and the loan-book panel reported **"Loans by tier — T1: 1965"** as though T2–T10 had issued nothing. **That reading was false**: `/root/keeper/copay.log` showed T2 and T3 rescues running the whole time. A keeper "fix" for the non-existent starvation was written, deployed and reverted before it did harm. **Rules: (1) a public array getter is uint256, never uint8; (2) prefer `getAllTiers()` — one call, no per-index guessing; (3) never wrap a contract read in an empty catch — log the error; (4) CHECK THE KEEPER LOG BEFORE BELIEVING A PANEL.** Same class as the earlier `memberPosition(address)` and `tierPairManagers(uint8)` guesses in check_reporters.js / tier_costs.js. `diag_cycleout_revert.js` still carries the bad ABI line but never calls it, so its elimination of the router-funding hypothesis stands. |
| **TWO DIFFERENT "RESERVES" — AND AN ON-CHAIN OVERRIDE THAT HID A WRONG BREAKDOWN (2026-07-27)** | The dashboard card said **"$10,000 reserved"** while the withdrawable modal showed a crossing reserve of **$2,217.50**. Both were correct — they are unrelated mechanisms sharing one word. **Crossing reserve** = half of each tier's entry fee, funded BY the entry fee, locked per seat toward that seat's next crossing (T1–T8 = 5+12.50+25+50+125+250+500+1250 = 2,217.50 exactly). **Automation reserve** = `TierRouter.reservedFor()`, EARNINGS withheld so auto-upgrade / auto re-entry can pay their own fee. Both are now separate tappable rows in the modal (owner: "the member should click to get the information"). **The real bug was in the frontend's model of `reservedFor`**: TierRouter:1494 is **ADDITIVE** — `if(reentry) +=curFee; if(autoUpgrade) +=nextFee; if(double) +=curFee` — but the frontend modelled it as seat-based either/or ("seat 1 upgrades, seat 2 re-enters"), which drops the standalone re-entry fee and gives $7,500 where the chain holds $10,000 at T8. **The dashboard then overrode the total with the on-chain value, so the TOTAL looked right while the itemised list the member reads was wrong** — a silent correction masking a wrong model, same shape as the empty-catch trap above. **Rule: when the frontend recomputes a contract figure, mirror the Solidity line-for-line and never let an on-chain override paper over the components.** |
| **Diagnostic scripts (NEW, masters in CryptoNova-Keepers (moved 2026-07-30, formerly CryptoNova-Keepers (formerly CryptoNite-MT5-Bots)))** | `pair_entries.js` (per-pair entries vs threshold + which pair each threshold selects) · `check_reporters.js` (where every bug reporter's wallet sits) · `set_threshold.js` / `set_route.js` (owner-guarded, simulate-first, log the revert command) · `integrity_check.js` (BFS guard, run after every deploy). |
| Live contract version | **V8.45 (deployed 2026-07-26 ~02:45 UTC)** — emergency fix of a V8.44 bug that corrupted a matrix's seat map (nested entry during rotation → phantom seat 128, occupancy drift +44, position 1 emptied → `F8V8: no root` wedge → 42 members could not self-rescue). Fix: seat resolved after rotation, never out of range, self-healing root scan. 402 tests green incl. new N1/N2 regressions. Frontend updated (115 replacements, commit 7e44268→e3176f6 on all 3 branches); gates preview 11:45 PM / main 12:00 AM EDT. Keepers on V8.45, **stress keeper OFF pending owner green light**. Integrity gate: `node /root/keeper/integrity_check.js` = INTEGRITY OK. Addresses: TierRouter 0xC44c1A51…, PairFactory 0x71020540…, MatrixKeeper 0x603db2d9… |
| PREVIOUS (dead) | V8.44 (deployed 2026-07-25, killed within hours by the nested-entry BFS corruption — see V8.45 above). V8.43 and earlier archived in archive/BUGS_PRE_V8_44_ARCHIVE.md. |
| Addresses file | **`deployed_addresses_v8_45.json`** — every keeper/diagnostic now genuinely defaults to this. **Fixed 2026-07-27:** 12 local scripts and 14 on the VPS still defaulted to v8_33–v8_44 (`direct_keeper` was 12 versions behind). Cron passes `ADDRESSES_FILE` so nothing was broken, but a MANUAL run without the env var read dead contracts and reported the results as fact — same failure shape as a silent catch, sourced from a default. All bumped; VPS backup at `/root/keeper/bak_2026-07-27/`. The old `diag_member_rescue.js` warning is retired. **Rule: when a version ships, grep both trees for stale `deployed_addresses_v8_*` defaults — the env var hides them.** |
| **Master-copy gaps (found 2026-07-27)** | `channel_pulse.js`, `diag_overflow.js` and `organic_drip.js` existed ONLY on the VPS with no copy in the master-scripts repo (now CryptoNova-Keepers) — invisible to review and lost with the box. Now pulled down and committed. NOTE: `diag_overflow.js` hardcodes `/root/keeper/...` absolute paths, so it runs on the VPS only. **Check for VPS-only scripts before assuming the repo is the full picture.** |
| **CO-PAY RESCUE IS AN EMERGENCY VALVE, NOT NORMAL OPERATION (owner, 2026-07-27)** | Co-pay rescue was **deliberately switched OFF** by design. It was re-enabled only as a workaround because SELF-rescue was broken, so testing could continue. **Do not read the SF loan book as the system's resting state** — $98,918 issued / $65,427 repaid / $33,491 outstanding is the cost of running the ambulance continuously for days, not evidence that the 3% SF split is too small. (Claude drew exactly that wrong conclusion from `sf_topups.log` before the owner corrected it.) **V8.46 success test: self-rescue works reliably → co-pay goes dormant → the SF stops lending → the outstanding book winds down as debts repay at cycle-out.** Co-pay stays in the contract as the emergency tool it was designed to be. |
| **CASCADE GAS — MEASURED AT LAST (V8.44 gate, never run before 2026-07-27)** | `test/V8_46_CascadeGas.test.js` (SIZE=127, both matrices full): plain entry 734,647 · peak entry during rotation 5,127,660 · **worst-case cascade 4,651,492** vs the 17.8M RPC cap. Hardhat caps one tx at 2^24 = 16,777,216, below the real cap, so a pass here is conservative. **STILL UNMEASURED: the multi-tier ladder cascade** (re-entry → upgrade into the next tier → double seat, each with its own rotation) — that is what the old "~15.5M full-matrix cascade" note meant, and it needs a multi-tier fixture. |
| **SILENT GRADUATIONS — CAUSE FOUND 2026-07-27: `require(!isInMatrix)` ON RE-ENTRY** | **SOLVED by fork replay** (`CryptoNova/scripts/replay_graduation.js`, FORK=1 TX=0x...). Replaying graduation tx `0xff488549…` (block 44702114) reproduced live gas to the unit (1,715,594) and traced 3 nested reverts, all **`Error("F8V8: already in matrix")`**, with **178,042 gas still left** — gas was never the cause. Chain: `MatB cycle-out → TierRouter 0xC44c1A51 → PairManager 0xD6df6ec0 → T4.1 MatA 0xfB1ef547 (occupancy 127/127) → require(!isInMatrix) at MatrixLogicLib:255 → REVERT → empty catch at :513 → member vanishes`. **ROOT CAUSE: a member can hold a seat in BOTH halves of the same pair at once.** Proven on `0xdFD9e186…`: 44686494 crossed MatA→MatB (seat in MatB); **44689351 took a SECOND seat in T4.1 MatA**; 44702114 MatB cycled out, re-entry targeted the MatA they already occupied, revert, gone. Note `MatrixKeeper.sol:558` already treats this exact string as expected-and-swallowable on the parked-rescue path — anticipated on one path, unhandled on the other. Confirmed count: **9 events, 6 members, $467.50 stranded**. |
| **V8.46 REWRITTEN AROUND THE REAL CAUSE (2026-07-27) — PREVENT, DON'T ACCOMMODATE** | **The fix is to stop duplicate seats forming, not to cope with them.** First attempt was the opposite — route re-entry to MatB when MatA is occupied — and `V8_46_SeatCollision.test.js` proved it dangerous: with a member in BOTH halves, the next MatA rotation makes them root, `_crossToPartner` tries to seat them in the MatB they occupy, `require(!isInMatrix)` reverts, and **that revert is NOT inside the swallowing try/catch** — it propagates out and **kills an unrelated member's `register()` transaction**. Trading a silent member loss for a pair-wide DoS. **Grim corollary: the silent graduation was MASKING this** — losing the member freed their MatB seat so the later crossing worked (0xdFD9e186 crossed clean at 44711526 only because it was graduated at 44702114). **SHIPPING SCOPE:** (1) **upgrade guard now checks BOTH halves** of the destination pair — `TierRouter._executeAdditive`, the primary fix; (2) `_sameTierTarget` collision check kept as defence in depth (limbo re-entry, co-pay rescue and manual registration can still seat directly); (3) **C** parks + emits `CycleOutFailed`; (4) **B is DEAD** — 178,042 gas left at the deepest frame. **SIZE: 24,433 bytes, 143 headroom** — paid for by deleting the dead `pairExpansionThreshold` + setter + event and converting **all 20** revert strings to existing custom errors (`TRState`/`TRAuth`/`TRBadValue`). **DO NOT dedupe the six `setTierGateThresholdT5..T10` wrappers — measured, it makes the contract BIGGER (24,809 → 25,024).** **FRONTEND MUST SHIP WITH IT:** `friendlyError()` needs `TRState` cases for register ("already registered") and upgrade ("tier not open yet"), plus `TRBadValue` on bulk upgrade — those were readable strings until now. **STILL TO BUILD: a two-tier fixture** driving a real auto-upgrade into a tier where the member already holds a MatB seat, asserting no duplicate forms. The existing test force-seats via an impersonated PairManager, which bypasses the guard, so it can only test survivability of a pre-existing duplicate. |
| **V8.46-C — SILENT GRADUATION (fix built + tested, ships with V8.46)** | `MatrixLogicLib:513` wrapped the additive engine in a try/catch with an **EMPTY catch**. The root is removed from the seat map BEFORE the call, so any revert inside `handleCycleOut` was swallowed — no re-entry, no upgrade, no park, **no event**. Members silently vanished. This is the same failure V8.44 was built to eliminate, reintroduced by the catch. **Measured: 5 wallets, 8 events, $267.50 stranded** (W1 lost T1/T2/T3; also the keeper wallet and 2 leader wallets — no community members). Fix: catch now parks the member and emits new `CycleOutFailed(member, tierIndex)`. Tests `V8_46_SilentGraduation.test.js` (G1/G2/G3) + mock `contracts/test/RevertingTierRouter.sol` all green. **Still unknown: WHY it reverted** — replay one of the 8 txs on a fork; if gas, V8.46-B's depth guard is the cure. Detector: `graduation_scan.js`. Per-member: `member_history.js`, `check_member_options.js`. |
| **STABILITY FUND — THREE TRAPS (all found 2026-07-27)** | (1) **`totalBalance` ≠ `balanceOf`.** Every spend guard reads the INTERNAL `totalBalance`; it only rises inside `receiveLayer()` / `receiveDebtRepayment()`. Reading `balanceOf` showed $52,075 while spendable was $75 and every rescue reverted `"SF: below floor"`. (2) **Direct transfers are PERMANENTLY LOST** — `ownerWithdraw` also caps at `totalBalance`, so $52,000 minted straight to the address is unrecoverable. (3) **Layer 1 carves to the CommunityWallet** — a $20,000 layer-1 deposit credited **$0.76**. **ALWAYS top up with `receiveLayer(tier, amount, 5)`** — layer 5 is the no-carve path. Use `topup_sf.js`, which now reports all three and retries through nonce contention. |
| **FUNDING CONSTRAINT (design fact, not a bug)** | The crossing reserve covers **exactly 50%** of the next entry fee; the member must have earned the other 50% or they PARK. Pool payout is fixed at 18% split across the matrix, so a member with **no referral income can essentially never self-fund a re-entry** — repeated parking with variable shortfalls is the DESIGNED path for them, not a fault. This is the honest answer to CryptoJan22's "$0.60 to $41.00" report. Referral income (5% L1 + 2.7%×5 chain) is what closes the gap. |
| Mainnet dates | Soft launch TBD (no countdown — opens by flag flip only); flagship June 19, 2027 |
| V8.44 GO-LIVE | **2026-07-25: preview 3:00 PM EDT, main 3:30 PM EDT — LIVE.** Frontend pushed to all 3 branches (33156be + gate commit 3dc9324); gates verified rendering. Owner Rabby registration test passed. 34/39 round-robin leaders registered via early access. VPS keepers all re-pointed to V8.44 + 8 cron lines active (incl. stress + channel_pulse). Health check: `node /root/keeper/v844_state.js`. First state @20:15 UTC: T1 MatA 88/127 rot=0 parked=0 — awaiting first cross into MatB (THE metric: MatB rot must climb, was frozen at 0 on V8.43). |
| Bigfill/stress keeper | **ON. Cron moved `*/5` → `*/30` on 2026-07-26** — runs were taking 60-93 min against a 5-min cron, so flock was discarding 12-18 ticks per run. Also added **`MAX_RESC` (default 120)**: rescues are now capped per run and the remainder rolls to the next one, instead of a single run trying to drain the whole parked backlog. Copay keeper (`*/10`) **gained a flock it never had** — it could previously overlap and collide on the keeper wallet's nonce. (Original notes below still apply.) State reset to wallet #0 for V8.43; ladder extended T1→T10 (deployer funds each rung; skips log `UPGR SKIP` if deployer USDC low — top up via `mint_deployer_usdc.js`, minted $20M 2026-07-23, balance ~$20.8M). Master copy: CryptoNova-Keepers/stress_keeper.js. Watch `/root/keeper/stress.log` + deployer Base Sepolia ETH. GAS: full-matrix cascade ≈15.5M+, public RPC caps tx gas <~17.8M (-32003) — ALL four cascade keepers (stress, manual_rescue, direct, frozen_matb; masters in CryptoNova-Keepers (moved 2026-07-30, formerly CryptoNova-Keepers (formerly CryptoNite-MT5-Bots))) use estimateGas ladder est×1.15→×1.05→est, never static limits. See MAINNET_TODO.md finding. |
| Working branch | `admin` |
| Admin frontend | https://admin.crypto-nova.app |
| Comp-plan docs | Synced to V8.43 contract truth 2026-07-23 (commit af8f569): faq/comp pages, all 10 locales, bot SYSTEM_PROMPT. Pushed to preview+main 2026-07-24 (508fa1f) along with gas fixes, standby-buffer UI, error-handler fix. |
| Marketing package | `CryptoNova-App/marketing/`: Member one-pager + 4 how-to PDFs + `CryptoNova_Explainer_Scripts_V8.md` (all contract-verified; QR/link placeholders pending). Old `CryptoNova_Video_Scripts.md` superseded. |
| V8.44 BUILD | **CONTRACTS COMPLETE 2026-07-25 (sandbox session, test-first, all suites green).** Full status table at TOP of CryptoNite-Smart-Contracts/CryptoNova/V8_44_PLAN.md. All fixes A–I built: escrow-funded additive engine + park-not-exit, overflow rework (own members → own MatB; rescueReentry/registerForMatB; rescueOverflow deleted), factory adminHandoff one-step ownership (Ownable2Step pendingOwner was the REAL orphan cause) + sweepMatrixOwnership + keeper WORK_FORCE_ROTATE backstop, pull-based pool (O(1) accumulators; withdrawableOf now includes pending accrual — FRONTEND note), _upgradeEligible unify + hybridUpgrade + registerWithOptions/Permit + bulkWithdraw + exitSeat (20% reserve penalty default — owner confirm), CW advanceEpoch permissionless + keeper auto-advance, adminReleaseStrandedReserve + enumerate_stranded_v843.js. Bonus fixes: missing softParkIdle wrapper (broken since V8.33), dead pendingCross deferral → now parks. NEXT: Windows full-suite run on real config → predeploy check → fresh V8.44 stress deploy (gates: MatB rot climbing all pairs keepers-off, 500+ rotations zero stranded, cascade gas < 17.8M) → THEN docs/bot/frontend sync (item F) vs deployed code. New tests: V8_44_CycleOut/Overflow/Keeper/PoolEquivalence/UX (.test.js). |

Superseded Active-state rows are preserved verbatim in `archive/CLAUDE_HISTORY.md`. One-off deploy scripts (`update_addrs_v8_xx.py`, `set_gate_v8_xx.py`, `patch_v835.py`) and stale index.html backups now live in `archive/`. ALL keeper/diagnostic script masters moved from CryptoNova-Keepers (formerly CryptoNite-MT5-Bots) to the dedicated `C:/CryptoNova-Keepers` repo on 2026-07-30 — new deploys still create their script at repo root.

## OPEN AT END OF 2026-07-26 (start here tomorrow)

1. **Push the frontend custom-error fix** — committed locally as `7b3c327`, **NOT pushed**. V8.44/V8.45 swapped revert strings for custom errors but the frontend ABIs were never updated, so ethers got `reason=null` and members saw "RPC may be busy" / "Transaction failed on-chain" for what were plain out-of-USDC errors. All 8 contract errors + the 2 OpenZeppelin ERC20 ones now declared, `friendlyError()` decodes them with real figures, and `bulkUpgrade` checks balance/allowance before blaming the network. Needs the 3-stage deploy + truncation check after each push.
2. **Community post** drafted at `community_post_2026-07-27.md`, **not sent**. Emoji version was approved; bounty line reads "tallied and paid out once we go live".
3. **BUGS.md: 6 still open** — the whole upgrade cluster is now explained (see item 1) but Lavern's "approved funds didn't appear" on 0x145805 (wallet holds $8,194, so NOT a funds problem) and @queensonnie's referral count are genuinely undiagnosed. 3 in Pending-Responded await member confirmation.
4. **Bounties: 13 accepted finds, $13 owed, all `paid_usd: 0`** — payment deferred to go-live by owner decision. Sherwyn 6, Kira 3, CryptoJan 2, Lavern-Gay 2.
5. **V8.46 contract fix** — see the table above.
6. Watch `T1.1/T1.2 MatA parked`: an entry into a FULL MatA rotates the queue (good) but the entrant parks (~1 park per rotation). Single digits is fine; dozens means the round-robin trade isn't worth it.

## Doctrine: code is truth

**The deployed contract is the single source of truth — never copy comp-plan facts from site pages, locale files, or the bot prompt without verifying against the Solidity source** (CryptoNite-Smart-Contracts/CryptoNova/contracts/). Verified V8.43 facts: 50% crossing reserve (funds HALF the crossing fee — crossing costs the full entry fee, earnings cover the rest, shortfall → parked → self-rescue with no debt) · 2.5% instant earn · 5% L1 · 2.7%×5 chain pay (L2–L6) · 18% equalization pool distributed EVERY ROTATION across seats 2–127 weighted by depth (NO root lump payout, NO "2× entry" payout — these are common stale/invented claims) · treasury 5% · SF 3% · dev+ops 1.5% · CW/buyback/liquidity 0.5% each · Whale Gate: T2–T5 open at 25 T5 pioneers, T6–T10 at own 25-member milestone; auto-upgrades never whale-gated BUT are throttled by the separate VELOCITY gate (`tierVelocityGreen`, keeper-set, flips dynamically — display as "Auto-Paused", never conflate with Whale Gate) · V8.43 additive cycle-out: re-entry → upgrade → double seat (defaults: re-entry ON, upgrade ON first 5 cycles) · pair overflow: deploy next pair at 375 entries, route ALL overflow at 381. NOTE: locale JSONs override HTML via i18n.js — fixing page HTML alone is not enough; non-EN locales currently carry corrected ENGLISH text for changed keys, retranslation pending.

## Branch → Domain map

| Branch | Domain | Who reviews | Rule |
|--------|--------|-------------|------|
| `admin` | admin.crypto-nova.app | Owner (personal assessment) | All work goes here first |
| `preview` | early.crypto-nova.app | QA team (quality assessment) | Push ONLY after admin is verified |
| `main` | crypto-nova.app / v8.crypto-nova.app | Community / world | Push ONLY after QA + leader sign-off |

## 3-stage deploy order — MANDATORY

```
Stage 1: git push origin admin
Stage 2: git push origin admin:preview --force   ← STOP, wait for QA
Stage 3: git push origin admin:main --force      ← STOP, wait for leader sign-off
```

**NEVER push to `main` before `preview`. NEVER skip stages.**  
**NEVER push to `preview` or `main` without explicit approval.**

## Timed launch-gate sequence (redeploy / relaunch)

The maintenance gate in `index.html` (~:502) is **host-based**: `admin.*` is always open (so testing is never gated); `early.*` counts down to `EARLY_MS` then shows the coupon bypass; main counts down to `MAIN_MS` then opens. One `index.html` serves all three — set both epoch-ms constants + the two pill labels and it's ready.

**Sequence (proven 2026-07-31 V8.46 redeploy):**
1. Push the cutover to **admin** → verify + register test accounts on the new contracts (admin bypasses the gate, so you test the real build ungated).
2. Take the live site offline at **midnight EDT**.
3. Gate **PREVIEW (early.)** to open **9:00 AM EDT** — set `EARLY_MS`.
4. Gate **MAIN** to open **10:00 AM EDT** — set `MAIN_MS`.
5. **Prep the gate in admin's index.html now, but do NOT push to preview/main until midnight.** At midnight: `git push origin admin:preview --force` then `git push origin admin:main --force`.

EDT = UTC-4 → 9 AM EDT = 13:00 UTC, 10 AM EDT = 14:00 UTC. Compute epoch-ms accordingly (e.g. Fri Jul 31 2026: EARLY_MS=1785502800000, MAIN_MS=1785506400000).


---

## After a CONTRACT deploy — verify on-chain + de-flag the domain (added 2026-07-31)

Every time new contracts are deployed:

1. **Verify EVERY contract on BaseScan** (`hardhat verify`; matrices need the `MatrixLogicLib` link). An unverified contract requesting token spending-caps from a fresh domain is the textbook shape of a drainer — Blockaid (the security engine behind MetaMask, Coinbase Wallet, Rainbow) auto-flags it and members get a full-screen "you will probably lose your assets to a scammer" warning and bail.
2. **Submit the domain + new contract addresses to Blockaid** at report.blockaid.io/mistake — AFTER verifying, so the re-scan sees clean contracts and the de-list sticks. One de-list clears the warning across every Blockaid-powered wallet.

Confirmed live 2026-07-30: `early.crypto-nova.app` + the CNOVA contract were flagged "Malicious site/address"; CNOVA was unverified on Basescan (the trigger). Aggressive ROI/MLM copy ("56% ROI", "refer 5 earn $2.50", "matrix") compounds it.

## CRITICAL: Post-push checklist — run EVERY TIME after git push

### Step 1 — Truncation check (ALWAYS FIRST, before anything else)

```powershell
# In the repo directory:
git show admin:index.html | Select-Object -Last 5
```

**For the preview and main stages you MUST check the REMOTE ref, not the local one.**
`git push origin admin:preview` updates the remote branch only — your local `preview`
ref never moves, so `git show preview:index.html` reads a stale local copy and will
happily "pass" while showing you a file from days ago. Caught live 2026-07-26: the
local refs were still on the countdown-gate commit and showed a completely different
file ending.

```powershell
git fetch origin
git show origin/preview:index.html | Select-Object -Last 5
git show origin/main:index.html    | Select-Object -Last 5
```

Or in bash:
```bash
tail -5 index.html
```

File **must** end with `</body></html>`. If it does not — it is truncated. Fix before checking RPC, wallet, or cache.

**DO NOT assume frozen spinners = wallet not connected or RPC slow. Check truncation first, every time.**

Correct index.html ending:
```
  console.log('[v8.8] ADDRS updated from deployed JSON');
}
</script>
<script src="/i18n.js" defer></script>
    <!-- Bug Report Footer -->
    <div style="text-align:center;...">🐛 Found a bug? Report it here</a></div>
</body></html>
```

### Step 2 — Hard refresh browser

`Ctrl + Shift + R` on each page. Vercel CDN caches aggressively; new builds take 1–3 min.

### Step 3 — Connect wallet

Live Stats on index.html require wallet connection. Spinners without a connected wallet are normal — not a bug.

---

## BUGS.md is API-owned — edit rules (added 2026-07-30)

**`api/submit-bug.js` commits member reports DIRECTLY to `admin` BUGS.md on GitHub, autonomously.** `api/get-reports.js` + `reports.html` read that same file. So BUGS.md has TWO authors: you (triage) and the API (member reports). Rules:

- **NEVER do a large local rewrite of BUGS.md while the form is live.** A big restructure merged against the API's report-commits can DROP member reports (a condense/merge did exactly this on 2026-07-30 — recovered from history). Do structural edits in a maintenance window with the form paused, or not at all.
- **Small triage edits only:** `git pull` immediately before, edit, commit, `git push` immediately after — minimize the race window.
- **Format is parser-critical:** open reports = `### [date] ...` blocks ABOVE the Resolved heading (one ticket each); resolved = 5-cell table rows BELOW it. `submit-bug.js` inserts at the Open-Issues heading and takes the FIRST match — keep that heading unique, unrenamed, first.

**STALE-CACHE TRAP (cost an hour on 2026-07-30):** the Cowork upload cache (`/mnt/user-data/uploads/...`) can serve an OLD copy of a file even after re-staging — it served a 76KB pre-condense BUGS.md while the real device file was the 25KB current one, leading to a false "reports were lost / API diverged" conclusion. **Verify file state with `device_bash` (`wc -c`, `grep`, `git show HEAD:file`) — the direct filesystem — NOT the upload cache, before drawing conclusions or editing.**

## File editing rules (mount sync lag)

The bash sandbox may serve stale/truncated content from the Windows mount for files it has already seen.

**Safe write procedure:**
1. Write content to a **new path** (e.g., `index.html.new`) using Python or the Write tool
2. `os.replace(new_path, original_path)` from Python, or `mv` from bash
3. Strip trailing null bytes afterward:

```python
data = open(path, 'rb').read().rstrip(b'\x00')
open(path, 'wb').write(data)
```

**Never** use `sed -i` on large HTML files — it reads stale cached content and writes it back truncated.

**CRLF:** Python writes use LF (`newline=''` or leave default). Git on Windows converts to CRLF on checkout — the LF warnings on `git add` are normal and harmless.

---

## Truncation fix procedure

If index.html is truncated after a push:

1. Get the full correct content from the last known-good git commit:
   ```bash
   git show <good-commit-sha>:index.html
   ```
2. Apply current address replacements via Python (do not re-use the truncated version)
3. Strip null bytes
4. Write to `index.html.new`, then `os.replace()` over `index.html`
5. Verify: `tail -5 index.html` → must show `</body></html>`
6. `git add index.html && git commit -m "fix: restore truncated index.html"`
7. `git push origin admin`

---

## Vercel rules

- **NEVER delete a Vercel project.** Wipes all env vars, domains, and settings permanently. There is no undo.
- After a force-push that Vercel ignores, use the "empty commit trick": `git commit --allow-empty -m "trigger deploy"` then push again.

---

## PowerShell rules

- **NEVER put keys or credentials in PowerShell commands.** All secrets live in `.env`.
- **PowerShell does not support `&&`.** Always give one command at a time. Wait for output + no-error confirmation before the next step.
- Runtime params only go on the command line: `HDR_OFFSET`, `COUNT`, `TIER`, etc.

---

## Common address variables in this repo

All contract addresses live in the `const ADDRS = {...}` block near the top of each HTML file. When a new version deploys, run the `update_addrs_vX_XX.py` script to replace all addresses in one pass.

**MANDATORY: `update_addrs_vX_XX.py` MUST include `api/telegram-qa.js` in its `ALL_FILES` list.**  
The Telegram bot has 3 hardcoded addresses (TIER_ROUTER, CNOVA_TOKEN, CNOVA_TREASURY) and a version label in the SYSTEM_PROMPT (`## Contracts (Base Sepolia — VX.XX)`) that go stale after every deploy. The script handles all of them automatically — do NOT skip it.

Always verify with truncation check afterward (`tail -5 index.html`).
